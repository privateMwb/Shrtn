import React from "react";
import ReactDOM from "react-dom/client";
import ShrtnApp from "./ShrtnApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ShrtnApp />
  </React.StrictMode>
);
